# ── API de autenticação (FastAPI + OAuth2 social + JWT próprio) ──────────────
fastapi>=0.110
uvicorn[standard]>=0.29
authlib>=1.3                # cliente OAuth2/OIDC (Google, Microsoft)
httpx>=0.27                 # transporte usado pelo Authlib
itsdangerous>=2.1          # SessionMiddleware (state do OAuth)
pyjwt>=2.8                  # emissão/validação do JWT próprio
sqlalchemy>=2.0            # ORM (User + licenças de módulo)
alembic>=1.13              # migrações de schema
pydantic-settings>=2.2     # configuração via .env
email-validator>=2.0       # validação de EmailStr (auth/admin schemas)
python-dotenv>=1.0
